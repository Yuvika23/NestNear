# .env (copy this file and rename to .env)
# During development, leave blank — Vite proxy handles it
VITE_API_URL=

# For production on Vercel, set this to your Render backend URL:
# VITE_API_URL=https://pg-finder-api.onrender.com/api
